{
  "name": "anima-team1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Homeward: discharge-readiness orchestrator on the NHS-SIM synthetic healthcare simulator",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "demo": "node scripts/demo-discharge.ts",
    "demo:detect": "node scripts/demo-discharge.ts --detect-only",
    "fallback": "node scripts/serve-fallback.ts",
    "quickstart": "node scripts/quickstart.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "evals": "node --test test/invariants.test.ts test/baseline.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@animahealth/adk": "^0.6.0",
    "openai": "^5.23.2",
    "zod": "^3.25.76"
  }
}
