{
  "name": "anima-team1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client for the NHS-SIM synthetic healthcare simulator API",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "quickstart": "node scripts/quickstart.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
